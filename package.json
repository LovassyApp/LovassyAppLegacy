{
    "name": "lovassy-app",
    "contributors": [
        "Nándor Ocskó <ocskon@gmail.com>",
        "Máté Gyimesi <gyim.mate@gmail.com>"
    ],
    "private": true,
    "devDependencies": {
        "lerna": "^4.0.0",
        "husky": "^7.0.0",
        "lint-staged": "^12.1.4"
    },
    "lint-staged": {
        "apps/boardlight-mobile/**.js": [
            "cd apps/boardlight-mobile && yarn run lint"
        ],
        "apps/boardlight-mobile/**": [
            "cd apps/boardlight-mobile && yarn run format"
        ],
        "apps/boardlight/**": [
            "cd apps/boardlight && yarn run lint"
        ]
    },
    "scripts": {
        "prepare": "husky install",
        "docker-build": "cd docker && docker-compose build app && docker-compose build",
        "blueboard-pkg": "cd docker && docker-compose up -d && docker exec -t llgapp-app composer update && docker-compose down --volumes -t 0",
        "blueboard-migrate": "cd docker && docker-compose up -d && docker exec -t llgapp-app php artisan migrate && docker-compose down --volumes -t 0",
        "docker-start": "cd docker && docker-compose up",
        "docker-down": "cd docker && docker-compose down --volumes -t 0"
    }
}
